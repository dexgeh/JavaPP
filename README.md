Java Pretty Printer Generator
=============================

Simple utility (both library and command line) to generate a pretty printing class.

An example of the invocation is in the ant build.xml, in the test target.

